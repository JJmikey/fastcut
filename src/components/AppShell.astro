---
// src/components/AppShell.astro
const currentYear = new Date().getFullYear();

// 接收參數，預設為 false (顯示 Footer)
const { cleanMode = false } = Astro.props;
---

<div class="flex flex-col min-h-screen bg-[#1e1e1e]">

  <!-- 
      1. 編輯器區域 (Editor Section) 
      永遠顯示
  -->
  <div class="h-screen flex flex-col overflow-hidden relative border-b border-gray-800">
    <slot name="header" />

    <main class="flex-1 flex overflow-hidden">
      <slot name="left-sidebar" />
      <section class="flex-1 flex flex-col min-w-0">
        <slot name="main" />
      </section>
      <slot name="right-sidebar" />
    </main>
  </div>

  <!-- 
      🔥 條件渲染：只有在 !cleanMode (非簡潔模式) 時才顯示下方內容
  -->
  { !cleanMode && (
    <>
      <!-- 2. SEO 內容區域 -->
      <article class="bg-[#020617] py-16 px-4 md:px-0 border-b border-slate-800">
        <slot name="seo-content">
            <div class="prose prose-invert max-w-4xl mx-auto">
                <h1 class="text-3xl font-bold text-white mb-6">Fast Video Cutter Editor</h1>
                <p class="text-gray-400">Loading editor content...</p>
            </div>
        </slot>
      </article>

      <!-- 3. 頁尾 -->
      <footer class="bg-[#020617] text-gray-500 py-12">
        <div class="max-w-6xl mx-auto px-4 grid grid-cols-2 md:grid-cols-4 gap-8 text-sm">
            <div class="space-y-4">
                <h4 class="text-white font-bold text-base">FastVideoCutter</h4>
                <p class="text-xs leading-relaxed">The secure, client-side video editor.</p>
            </div>
            <div class="space-y-4">
                <h4 class="text-white font-bold">Tools</h4>
                <ul class="space-y-2">
                    <li><a href="/editor" class="hover:text-cyan-400 transition-colors">Video Cutter</a></li>
                    <li><a href="/add-audio-to-video" class="hover:text-cyan-400 transition-colors">Add Audio to Video</a></li>
                </ul>
            </div>
            <div class="space-y-4">
                <h4 class="text-white font-bold">Support</h4>
                <ul class="space-y-2">
                    <li><a href="#" class="hover:text-cyan-400 transition-colors">How-to Guide</a></li>
                </ul>
            </div>
            <div class="space-y-4">
                <h4 class="text-white font-bold">Legal</h4>
                <ul class="space-y-2">
                    <li><a href="#" class="hover:text-cyan-400 transition-colors">Privacy Policy</a></li>
                </ul>
            </div>
        </div>
        <div class="text-center mt-12 pt-8 border-t border-slate-800 text-xs">
            &copy; {currentYear} FastVideoCutter.com. All rights reserved.
        </div>
      </footer>
    </>
  )}

</div>