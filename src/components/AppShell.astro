---
import Footer from './Footer.astro';

// 接收參數
const { cleanMode = false } = Astro.props;
const currentYear = new Date().getFullYear();
---

<div class="flex flex-col min-h-screen bg-[#1e1e1e]">

  <!-- 
      🔥 1. 桌面版編輯器 (Desktop Editor) 
      關鍵類別：hidden md:flex
      解釋：預設隱藏 (hidden)，在中型螢幕以上 (md:) 變為彈性盒顯示 (flex)。
  -->
  <div class="hidden md:flex h-screen flex-col overflow-hidden relative border-b border-gray-800">
    <slot name="header" />

    <main class="flex-1 flex overflow-hidden">
      <slot name="left-sidebar" />
      <section class="flex-1 flex flex-col min-w-0">
        <slot name="main" />
      </section>
      <slot name="right-sidebar" />
    </main>
  </div>

  <!-- 
      🔥 2. 手機版擋板 (Mobile Blocker)
      關鍵類別：flex md:hidden
      解釋：預設顯示 (flex)，在中型螢幕以上 (md:) 隱藏 (hidden)。
  -->
  <div class="flex md:hidden h-[60vh] flex-col items-center justify-center text-center px-6 bg-[#0f172a] border-b border-slate-800">
    <div class="w-16 h-16 bg-slate-800 rounded-2xl flex items-center justify-center mb-6 shadow-lg shadow-cyan-500/10">
        <svg xmlns="http://www.w3.org/2000/svg" width="32" height="32" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" class="text-cyan-400"><rect width="20" height="14" x="2" y="3" rx="2"/><line x1="8" x2="16" y1="21" y2="21"/><line x1="12" x2="12" y1="17" y2="21"/></svg>
    </div>
    <h2 class="text-2xl font-bold text-white mb-3">Desktop Experience Only</h2>
    <p class="text-slate-400 max-w-xs leading-relaxed">
        FastVideoCutter is a powerful professional editor. 
        For the best experience and performance, please open this tool on your <strong>Desktop or Laptop</strong>.
    </p>
  </div>

  <!-- 
      3. 下方內容區 (SEO + Footer)
      這裡保持不變，手機版依然可以看到這些文章，對 SEO 很有幫助
  -->
  { !cleanMode && (
    <>
      <article class="bg-[#020617] py-16 px-4 md:px-0 border-b border-slate-800">
        <slot name="seo-content">
            <div class="prose prose-invert max-w-4xl mx-auto">
                <h1 class="text-3xl font-bold text-white mb-6">Fast Video Cutter Editor</h1>
                <p class="text-gray-400">Loading editor content...</p>
            </div>
        </slot>
      </article>

      <Footer />
    </>
  )}

</div>