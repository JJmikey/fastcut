---
import FileUploader from './FileUploader.svelte';
---

<!-- 
  修改說明：
  1. 移除了原本寫死的 HTML Tabs (Media / AI Tools)。
  2. 移除了內層 div 的 padding (p-4)，因為 FileUploader 內部會自己處理排版。
  3. 這樣 FileUploader 裡的 Tabs 才能貼齊邊緣，比較好看。
-->
<aside class="w-[320px] flex flex-col border-r border-gray-700 bg-[#181818] flex-shrink-0 h-full overflow-hidden">
  <!-- 直接載入 Svelte 組件，讓它佔滿空間 -->
  <FileUploader client:load />
</aside>